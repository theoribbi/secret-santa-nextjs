# Copy this file to docker.env and adjust the values as needed

# Database Configuration
POSTGRES_DB=secret_santa
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Development: PostgreSQL via Docker
DATABASE_URL=postgresql://postgres:password@db:5432/secret_santa

# Production: Use Neon connection string (example):
# DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Next.js Configuration
NODE_ENV=development

# URL de l'application (utilisée côté serveur pour les emails)
# En production sur Vercel, cette variable est auto-détectée via les headers
# Définir explicitement si besoin (ex: domaine custom)
APP_URL=http://localhost:3000

# URL publique (utilisée côté client) - injectée au build time
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Ports
APP_PORT=3000
DB_PORT=5432
STUDIO_PORT=4983

# Drizzle Studio Gateway
STUDIO_MASTER_PASSWORD=drizzle123

# Image Upload Configuration
# Dev: images are stored locally in /uploads
# Prod: images are stored in Vercel Blob
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here
NEXT_PUBLIC_UPLOADS_URL=http://localhost:3000/uploads

# Resend Configuration for Email Sending
# Get your API key at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_FROM_NAME=Secret Santa

# Note: En production, utilise ton propre domaine vérifié sur Resend
# RESEND_FROM_EMAIL=noreply@tondomaine.com
